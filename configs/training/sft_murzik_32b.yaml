### NULLXES MURZIK-32B — Supervised Fine-Tuning (chat)

model_name_or_path: /workspace/checkpoints/pt-32b/checkpoint-best
trust_remote_code: true

stage: sft
do_train: true
finetuning_type: full
deepspeed: configs/training/ds_zero3_moe.json

dataset: murzik_sft
dataset_dir: /workspace/data/sft
template: murzik
cutoff_len: 8192
preprocessing_num_workers: 8

output_dir: /workspace/checkpoints/sft-32b
logging_steps: 10
save_steps: 200
save_total_limit: 3
report_to: wandb
run_name: murzik-sft-32b

per_device_train_batch_size: 1
gradient_accumulation_steps: 4
learning_rate: 8.0e-6
num_train_epochs: 2
lr_scheduler_type: cosine
warmup_ratio: 0.03
weight_decay: 0.01
bf16: true
gradient_checkpointing: true

moe_aux_loss_coef: 0.01

seed: 42
